using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TestPlayerData
{
    [StringValue("PlayerName")]
    PLAYER_NAME,
    [StringValue("GameMode")]
    GAME_MODE,
    [StringValue("Map")]
    MAP,
}
